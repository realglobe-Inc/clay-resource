/**
 * Test case for throwMix.
 * Runs with mocha.
 */
'use strict'

const throwMix = require('../lib/mixins/throw_mix.js')
const assert = require('assert')
const co = require('co')

describe('throw-mix', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Throw mix', () => co(function * () {

  }))
})

/* global describe, before, after, it */
