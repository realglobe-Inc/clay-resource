/**
 * Test case for conditionMix.
 * Runs with mocha.
 */
'use strict'

const conditionMix = require('../lib/mixins/condition_mix.js')
const assert = require('assert')
const co = require('co')

describe('condition-mix', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Condition mix', () => co(function * () {

  }))
})

/* global describe, before, after, it */
