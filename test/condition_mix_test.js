/**
 * Test case for conditionMix.
 * Runs with mocha.
 */
'use strict'

const conditionMix = require('../lib/mixins/condition_mix.js')
const assert = require('assert')

describe('condition-mix', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Condition mix', async () => {

  })
})

/* global describe, before, after, it */
