/**
 * Test case for cacheMix.
 * Runs with mocha.
 */
'use strict'

const cacheMix = require('../lib/mixins/cache_mix.js')
const assert = require('assert')

describe('cache-mix', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Cache mix', async () => {

  })
})

/* global describe, before, after, it */
