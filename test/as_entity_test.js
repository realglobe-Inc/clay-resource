/**
 * Test case for asEntity.
 * Runs with mocha.
 */
'use strict'

const asEntity = require('../lib/helpers/as_entity.js')
const assert = require('assert')
const co = require('co')

describe('as-entity', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('As entity', () => co(function * () {

  }))
})

/* global describe, before, after, it */
