/**
 * Test case for entityMix.
 * Runs with mocha.
 */
'use strict'

const entityMix = require('../lib/mixins/entity_mix.js')
const assert = require('assert')
const co = require('co')

describe('entity-mix', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Entity mix', () => co(function * () {

  }))
})

/* global describe, before, after, it */
