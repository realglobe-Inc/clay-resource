/**
 * Test case for clusterMix.
 * Runs with mocha.
 */
'use strict'

const clusterMix = require('../lib/mixins/cluster_mix.js')
const assert = require('assert')

describe('cluster-mix', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Cluster mix', async () => {

  })
})

/* global describe, before, after, it */
