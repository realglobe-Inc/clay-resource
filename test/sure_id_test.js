/**
 * Test case for sureId.
 * Runs with mocha.
 */
'use strict'

const sureId = require('../lib/helpers/sure_id.js')
const assert = require('assert')


describe('sure-id', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Sure id', async () => {

  })
})

/* global describe, before, after, it */
