/**
 * Test case for policyInbound.
 * Runs with mocha.
 */
'use strict'

const policyInbound = require('../lib/inbounds/policy_inbound.js')
const assert = require('assert')


describe('policy-inbound', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Policy inbound', async () => {

  })
})

/* global describe, before, after, it */
