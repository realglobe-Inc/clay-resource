/**
 * Test case for policyInbound.
 * Runs with mocha.
 */
'use strict'

const policyInbound = require('../lib/inbounds/policy_inbound.js')
const assert = require('assert')
const co = require('co')

describe('policy-inbound', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Policy inbound', () => co(function * () {

  }))
})

/* global describe, before, after, it */
